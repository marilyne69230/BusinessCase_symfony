{% extends 'base.html.twig' %}

{% block title %}ETH{% endblock %}

{% block body %}
    <h1>ETH</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ e_t_h.id }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ e_t_h.price }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ e_t_h.date ? e_t_h.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_e_t_h_index') }}">back to list</a>

    <a href="{{ path('app_e_t_h_edit', {'id': e_t_h.id}) }}">edit</a>

    {{ include('eth/_delete_form.html.twig') }}
{% endblock %}
