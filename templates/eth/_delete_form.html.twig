<form method="post" action="{{ path('app_e_t_h_delete', {'id': e_t_h.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ e_t_h.id) }}">
    <button class="btn">Delete</button>
</form>
